{
  "name": "shared-kernel",
  "main": "./built/index.ts",
  "types": "./built/index.d.ts",
  "version": "1.0.52",
  "private": true,
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "rm -rf ./built && tsc -p ./tsconfig.build.json && tsconfig-replace-paths -p tsconfig.build.json",
    "dev": "nodemon -e ts,js --exec 'ts-node -r tsconfig-paths/register' ./index.ts",
    "test": "jest",
    "postinstall": "cd ../node_modules/invest-nodejs-grpc-sdk && tsc && cd ../.."
  },
  "dependencies": {
    "@foxify/events": "^2.1.0",
    "dotenv": "^16.0.0",
    "fast-safe-stringify": "^2.1.1",
    "inversify": "^6.0.1",
    "invest-nodejs-grpc-sdk": "git+https://github.com/mtvkand/invest-nodejs-grpc-sdk.git",
    "mathjs": "^10.6.1",
    "reflect-metadata": "^0.1.13",
    "typescript": "4.6.3",
    "winston": "^3.7.2",
    "winston-telegram": "^2.5.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "@types/node": "^18.0.0",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node": "10.7.0",
    "tsconfig-replace-paths": "^0.0.11"
  }
}
